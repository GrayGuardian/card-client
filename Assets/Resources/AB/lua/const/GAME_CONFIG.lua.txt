local GAME_CONFIG = {}

-- 角色栏位最大数量
GAME_CONFIG.MAX_PLAYER_SUM = 3
-- 卡牌强化最大等级
GAME_CONFIG.MAX_CARD_LV = 109

-- 背包配置
GAME_CONFIG.BAG_CONFIG = {
    PAGE_SUM = 0 -- 背包单分页物品数量
}
-- 默认屏幕分辨率
GAME_CONFIG.DEFAULT_SCREEN = {WIDTH = 1080, HEIGHT = 2340}
GAME_CONFIG.SCREEN_SCALE =
    (UE.Screen.width / UE.Screen.height) / (GAME_CONFIG.DEFAULT_SCREEN.WIDTH / GAME_CONFIG.DEFAULT_SCREEN.HEIGHT)
return GAME_CONFIG
